# OpenCode Session — 2026-06-30

**Model:** deepseek-v4-flash-free (opencode/deepseek-v4-flash-free)

## Changes

### Directory Layout
- Removed `models/` wrapper directory
- Flat layout: `gguf/`, `mlx/`, `safetensors/`, `provider-configs/` in shelf root
- `model-shelf.json` + `registry.json` in shelf root
- `ms init` auto-creates `model-shelf/` subfolder when parent path given

### Code (`src/model_shelf/cli.py`)
- `format_path()`: `f"{fmt}/{publisher}/{name}"` (no `models/` prefix)
- `default_config()`: paths updated for flat layout
- `find_shelf_root()`: checks for `registry.json` (not `models/registry.json`)
- `read_registry()` + `add()`: use `registry.json` in shelf root
- `provider_path()`: default models path is shelf root

### HuggingFace Search
- New `search_huggingface()` hits HF public API
- `ms search` falls back to HF when local registry empty
- `--remote` / `--no-local` flags for explicit control
- Results tagged with `(HF)` in output

### Install `--source` Fix
- `--source` accepts HF URL for direct install (bypasses registry)
- Also works as registry-based install with model name query

### Tests
- 3 test files: `test_layout.py`, `test_search.py`, `test_install.py`
- 22 tests, all passing
- Fixtures for temp shelf + pre-populated registry
- Dev dependency: `pytest>=8`
